Screenshots will be added here. To generate them:

1. Run `docker compose up --build`
2. Open http://localhost:5173
3. Submit some jobs and workflows via the API
4. Capture screenshots of:
   - Jobs page → `dashboard-jobs.png`
   - Workflows page → `dashboard-workflows.png`
   - Workers page → `dashboard-workers.png`
